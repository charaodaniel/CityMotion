/**
 * CityMotion — Página: Veículos (Frota)
 *
 * Refatorado em módulos dentro de ./veiculos/:
 *   index.js  → Página principal (estado, CRUD, eventos)
 *   modals.js → Renderizadores de modais (HTML puro)
 */
export { default } from './veiculos/index.js';
