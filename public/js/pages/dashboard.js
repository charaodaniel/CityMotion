/**
 * CityMotion — Página: Dashboard
 *
 * Refatorado em módulos dentro de ./dashboard/:
 *   index.js  → Página principal (orquestração, subscriptions)
 *   modals.js → Renderizadores HTML (cards, tabelas)
 */
export { default } from './dashboard/index.js';
